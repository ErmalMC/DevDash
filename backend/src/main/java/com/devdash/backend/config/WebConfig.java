package com.devdash.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS is centralized in SecurityConfig.corsConfigurationSource().
}